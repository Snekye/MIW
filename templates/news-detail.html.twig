{% extends 'base.html.twig' %}

{% block stylesheets %}
<link rel="stylesheet" href="{{asset('css/component/news.css')}}">
{% endblock %}

{% block body %}
<a href="../news">Voir toutes les actualités</a>
    {% set n = news.0 %}
    <div class="component-news">
        <img src="{{ vich_uploader_asset(n, 'imageFile') | imagine_filter('partner') }}" alt="{{ n.imageName }}">
        <p>{{ n.date|date('d-m-Y') }} - {{ n.titre }}</p>
        <p>{{ n.contenu }}</p>
    </div>
{% endblock %}